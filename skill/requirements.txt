# torch 的 +cpu 变体只在 PyTorch 官方 CPU index 发布，需显式 extra index。
--extra-index-url https://download.pytorch.org/whl/cpu
openvino>=2025.4
nncf>=2.15.0
torch==2.8.0+cpu
torchvision==0.23.0+cpu
torchaudio==2.8.0+cpu
# NOTE: skill 运行只需要 openvino + nncf（nncf 用于 OCR 加载）。
# optimum-intel 仅供 model-convert/（转换/发布模型）使用，已从运行依赖移除，
# 以免依赖上游某个漂移的 git revision 导致安装失败。
modelscope
Pillow
scipy
numpy<2.0
transformers==4.57.6
qwen-vl-utils
qwen-tts
librosa
soundfile
# QR sticker generation + A4 printable label PDF (create_sticker.py)
segno>=1.6.1
reportlab>=4.0.0
